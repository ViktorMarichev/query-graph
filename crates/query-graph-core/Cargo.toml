[package]
description = "Database-independent query graph planner and SQL compiler core"
edition     = "2021"
license     = "MIT"
name        = "query-graph-core"
publish     = false
version     = "1.1.2"

[dependencies]
serde      = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
